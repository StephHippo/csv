begin

  #open file
  #read in each line
    #split the line by spaces
    #create validator object, pass in array
    #validator has a status variable and the same fields as an entry
    #if valid status variable
      #add to entry array
    #else
      #log out bad entry
  #convert all valid entries
  #print entries

end